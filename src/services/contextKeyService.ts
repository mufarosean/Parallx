// contextKeyService.ts — IContextKeyService interface + implementation
//
// Re-exports the ContextKeyService implementation so it can be
// instantiated during workbench service registration (Phase 1).

export { ContextKeyService } from '../context/contextKey.js';
